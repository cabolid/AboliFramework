<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">

    <!-- Properties -->
    <Properties>
        <Property name="basepath">./logs</Property>
        <Property name="logfileName">${basepath}/app.log</Property>
        <Property name="logfilePattern">${basepath}/app-%d{yyyy-MM-dd}-%i.log.gz</Property>
    </Properties>

    <!-- Appenders -->
    <Appenders>

        <!-- Console Appender -->
        <Console name="ConsoleLogger" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- Rolling File Appender -->
        <RollingFile name="RollingFileLogger"
                     fileName="${logfileName}"
                     filePattern="${logfilePattern}">
            <PatternLayout>
                <Pattern>%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n</Pattern>
            </PatternLayout>

            <Policies>
                <!-- Rotate on file size OR date -->
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>

            <DefaultRolloverStrategy max="5"/>
        </RollingFile>

    </Appenders>

    <!-- Loggers -->
    <Loggers>

        <!-- Root logger -->
        <Root level="info">
            <AppenderRef ref="ConsoleLogger"/>
            <AppenderRef ref="RollingFileLogger"/>
        </Root>

    </Loggers>
</Configuration>
